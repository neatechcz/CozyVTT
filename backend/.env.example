# CozyVTT Backend — Local Development Only
# -------------------------------------------------------
# This file is ONLY needed when running the backend directly
# with "npm run dev" (outside of Docker).
#
# If you are using Docker (docker compose up -d), configure
# everything in the ROOT .env file instead — do not edit this.
# -------------------------------------------------------
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ----------------------------------------------
# Database — required
# ----------------------------------------------
DATABASE_URL="postgresql://cozyvtt:CHANGE_ME_TO_SECURE_PASSWORD@localhost:5432/cozyvtt"

# ----------------------------------------------
# Application
# ----------------------------------------------
NODE_ENV=development
PORT=4000

# ----------------------------------------------
# Security Secrets — REQUIRED
# Generate with: openssl rand -hex 32
# Using placeholder values causes the server to refuse to start in production.
# ----------------------------------------------
SESSION_SECRET=CHANGE_ME_TO_RANDOM_STRING_32_CHARS_MIN

# ----------------------------------------------
# CORS — set to your frontend origin
# ----------------------------------------------
CORS_ORIGIN=http://localhost:3000

# ----------------------------------------------
# File Upload Limits (MB)
# ----------------------------------------------
MAX_MAP_SIZE_MB=50
MAX_TOKEN_SIZE_MB=5
MAX_AUDIO_SIZE_MB=20
MAX_AVATAR_SIZE_MB=2

# ----------------------------------------------
# Session Timeouts (milliseconds)
# ----------------------------------------------
SESSION_MAX_AGE=3600000
REMEMBER_ME_MAX_AGE=2592000000

# ----------------------------------------------
# Rate Limiting
# ----------------------------------------------
RATE_LIMIT_MAX_REQUESTS=300

# Number of reverse proxies in front of the backend that append
# X-Forwarded-For (default 1). Too low: every client shares one rate-limit
# bucket. Too high: clients can spoof their address. Host Nginx + gateway = 2.
# TRUST_PROXY_HOPS=1

# ----------------------------------------------
# SMTP — optional (leave blank to disable email)
# ----------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=CozyVTT <noreply@localhost>
SMTP_SECURE=false
APP_URL=http://localhost:3000

# ----------------------------------------------
# Logging
# ----------------------------------------------
# LOG_LEVEL=debug
